#include <stdio.h>
#include <string.h>
//defining a variable 1: using #define

#define true 1
#define false 0

int main(){
    //types:
    /*
        Whole numbers(Integers)- char, short, int, long, long long
        Whole positive numbers(Unsigned integers)- unsigned char,unsigned short,unsigned int,unsigned long,unsigned long long
        Real numbers(floating point numbers)- float, double
        Structures- (arrays, string, struct and so on) 
        Note: bool does not exist in c
    */
    //defining a variable 2:
    int foo = 1;
    double bar;


    //Exercise
}